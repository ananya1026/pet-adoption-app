services:
  backend:
    build: ./backend
    container_name: petadopt-backend
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    networks:
      - petadopt-network
networks:
  petadopt-network:
    external: true
volumes:
  mongo_data: